# Core UI & data handling
PySide6>=6.6.0
pyqtgraph>=0.13.3
numpy>=1.24.0

# Optional: Snap7 PLC communication
python-snap7>=1.0.0

# For advanced animations (if needed)
# PyQt6 might be used instead, but we stick to PySide6